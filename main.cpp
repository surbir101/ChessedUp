#include <iostream>
#include "Piece.h"
#include "Pawn.h"
#include "Bishop.h"
#include "Queen.h"
#include "King.h"
#include "Board.h"
#include <string>
#include <vector>

using namespace std;


int main()
{

  Board board ;

  Spot* ptr1 ;
  Spot* ptr2 ;




/*

 for(int i =0 ; i< 8; i++)
  {
    cout <<  board.boxes[0][i]->getPiece()->canMove(&board,ptr1,ptr2) << endl ;

  }
     for(int i =0 ; i< 8; i++)
  {
    cout <<   board.boxes[1][i]->getPiece()->canMove(&board,ptr1,ptr2) << endl ;

  }
   for(int i =0 ; i< 8; i++)
  {
     cout <<  board.boxes[7][i]->getPiece()->canMove(&board,ptr1,ptr2) << endl ;

  }
   for(int i =0 ; i< 8; i++)
  {
    cout <<   board.boxes[6][i]->getPiece()->canMove(&board,ptr1,ptr2) << endl ;

  }


*/


        return 0;
}
